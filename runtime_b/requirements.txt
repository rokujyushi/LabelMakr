hydra-core==1.3.2
lightning==2.1.2
numpy>=2.4,<3
omegaconf==2.3.0
PyYAML>=6.0.2,<7
TextGrid==1.5

# Installed separately by setup_CPU.bat / setup_GPU.bat
# torch
# torchaudio
# torchvision

# Provisioned separately during packaging / setup
# pydomino
